<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="co.yedam.mapper.MemberMapper">
	<insert id="insertMember" parameterType="co.yedam.vo.MemberVO">
		<choose>
		  <when test="responsibility.length() == 0">
			insert into member_tbl(user_id, user_pw, user_name)
			values (#{userId}, #{userPw}, #{userName})
		  </when>
		  <otherwise>
		  	insert into member_tbl(user_id, user_pw, user_name, responsibility)
		  	values (#{userId}, #{userPw}, #{userName}, 'Admin')
		  </otherwise>
		</choose>
	</insert>
</mapper>